import PremiumLoader from '@/components/common/PremiumLoader';

export default function Loading() {
    return (
        <PremiumLoader
            label="Loading application"
            subLabel="Please wait while we prepare your page"
        />
    );
}
